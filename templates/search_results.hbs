<div class="correct-search is-hidden">
  {{search instant=true submit=true placeholder=settings.search_placeholder}}
</div>

<div class="container search-results-page" data-search-results>
  <div class="container-inner">
    <h4 class="section-header section-header">
      <b>
        Search Results
      </b>
    </h4>
    <p class="search-results-count">{{t 'results' query=query count=results_count}}</p>

    <div class="row search-results clearfix">
      <section class="column column--sm-6 search-results-column">
        <div class="search-results-subheading">
          <h2 class="h3 search-results-subheading__title">{{t 'knowledge_base'}}</h2>
        </div>
        {{#if article_results}}
          <ul class="list-unstyled search-results-list">
            {{#each article_results}}
              <li class="search-result">
                <ol class="breadcrumbs breadcrumbs--search-results">
                  {{#each path_steps}}
                    <li title="{{name}}"><a href="{{url}}">{{name}}</a></li>
                  {{/each}}
                </ol>

                <h3 class="h5 search-result__title">
                <a href="{{url}}" class="search-result-link">{{title}}</a>
                  {{#if vote_sum}}
                    <span class="search-result-votes"><span class="fas fa-thumbs-up"></span> {{vote_sum}}</span>
                  {{/if}}</h3>

                <p class="search-result__description">{{text}}</p>

                <div class="meta search-result__meta">
                    <span class="meta__item">{{date created_at}}</span>
                    </span>
                  </div>
              </li>
            {{/each}}
          </ul>
        {{else}}
          <p>
            {{t 'no_results' query=query}}
            {{#link 'help_center'}}
              {{t 'browse_knowledge_base'}}
            {{/link}}
          </p>
        {{/if}}
      </section>
    </div>
    <div class="text-center">
      {{pagination}}
    </div>
  </div>
</div>
